#include "main.h"

/**
 * _islower - checking for lowercase alphabets
 *
 * @c: checks the lower case
 *
 * Return: 1 if lower, 0 if not lowercase
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
