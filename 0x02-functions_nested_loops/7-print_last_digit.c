#include "main.h"

/**
 * print_last_digit - prints the last digit
 *
 * @last: prints last digit
 *
 * Return: last digit
 */

int print_last_digit(int last)
{
	last = last % 10;

	if (last < 0)
		last = -last;
	_putchar(n + '0');

	return (last);
}
